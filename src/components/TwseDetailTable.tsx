
export default function TwseDayTable({ date, stockNo }: { date: string[], stockNo: string }) {

    console.log(date);
    console.log(stockNo);
    return (
        <div>
          
        </div>
    );
}